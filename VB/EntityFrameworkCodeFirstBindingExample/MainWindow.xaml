<Window x:Class="EntityFrameworkCodeFirstBindingExample.MainWindow"
        DataContext="{dxmvvm:ViewModelSource {x:Type local:MainViewModel}}" 
        mc:Ignorable="d" Title="Scheduling Application" 
        Height="600" 
        Width="800" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local="clr-namespace:EntityFrameworkCodeFirstBindingExample" 
    xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduling" 
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm">

    <Grid>
        <dxsch:Scheduler x:Name="scheduler" GroupType="Resource">
            
            <!--region #Behaviors-->
            <dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="AppointmentsUpdated" Command="{Binding AppointmentsUpdatedCommand}" />
                <dxmvvm:EventToCommand EventName="InitNewAppointment" Command="{Binding InitNewAppointmentCommand}" PassEventArgsToCommand="True" />
            </dxmvvm:Interaction.Behaviors>
            <!--endregion #Behaviors-->

            <!--region #DataSource-->
            <dxsch:Scheduler.DataSource>
                <dxsch:DataSource AppointmentsSource="{Binding Appointments}" ResourcesSource="{Binding Resources}">
                    <dxsch:DataSource.AppointmentMappings>
                        <dxsch:AppointmentMappings Start="StartTime" 
                                                   End="EndTime" 
                                                   AllDay="AllDay" 
                                                   Subject="Subject" 
                                                   AppointmentId="ID" 
                                                   Description="Description" 
                                                   LabelKey="Label" 
                                                   Location="Location" 
                                                   RecurrenceInfo="RecurrenceInfo" 
                                                   Reminder="ReminderInfo" 
                                                   ResourceId="CarId" 
                                                   StatusKey="Status" 
                                                   Type="EventType" />
                    </dxsch:DataSource.AppointmentMappings>
                    <dxsch:DataSource.ResourceMappings>
                        <dxsch:ResourceMappings Id="ID" 
                                                Caption="Trademark" />
                    </dxsch:DataSource.ResourceMappings>
                </dxsch:DataSource>
            </dxsch:Scheduler.DataSource>
            <dxsch:DayView ResourcesPerPage="3" />
            <dxsch:MonthView/>
            <!--endregion #DataSource-->
        </dxsch:Scheduler>

    </Grid>
</Window>
